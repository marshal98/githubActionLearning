name: 01-Building Blocks

on:
  push:
    branches:
      - main

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Show the trigger
              run: echo "I have been triggered bu a (n) ${{github.event_name}}."
                        